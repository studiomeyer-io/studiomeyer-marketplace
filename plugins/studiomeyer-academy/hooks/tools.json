{
  "_comment": "Input schemas of the Academy tools this plugin drives from a hook. Read off mcp-academy src/account-tools.ts. These tools need ACADEMY_API_KEY. Without it the server exposes 12 public tools and none of these.",
  "academy_quiz": {
    "properties": {
      "slug": { "type": "string" },
      "level": { "type": "number" },
      "lessonSlug": { "type": "string" },
      "locale": { "type": "string" }
    },
    "required": []
  },
  "academy_progress_complete": {
    "properties": {
      "level": { "type": "number" },
      "slug": { "type": "string" },
      "locale": { "type": "string" }
    },
    "required": ["level", "slug"]
  }
}
